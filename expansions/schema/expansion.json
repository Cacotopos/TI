{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Expansion Site Config",
  "type": "object",
  "required": ["id", "name", "version"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "version": { "type": "string" },
    "description": { "type": "string" },
    "release_date": { "type": "string" },
    "source": {
      "type": "object",
      "properties": {
        "images": { "type": "string" },
        "icons": { "type": "string" }
      }
    },
    "sections": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "type"],
        "properties": {
          "id": { "type": "string" },
          "title": { "type": "string" },
          "type": { "type": "string", "enum": ["markdown", "cards", "list"] },
          "content": { "type": "string" },
          "items": { "type": "array" }
        }
      }
    },
    "assets": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "component": { "type": "string", "enum": ["us-mini", "tarot", "poker", "player-sheet", "token", "other"] },
          "isCard": { "type": "boolean" }
        }
      }
    }
  }
}
